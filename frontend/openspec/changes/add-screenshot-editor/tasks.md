## 1. 基础设施搭建

- [x] 1.1 安装 fabric.js 依赖：`npm install fabric @types/fabric`
- [x] 1.2 在 App.tsx 中添加 `?window=editor` 路由判断，创建 ScreenEditor 组件骨架
- [x] 1.3 在 electron/main.ts 中添加 `createEditorWindow` 函数，加载 `${DEV_SERVER_URL}?window=editor`
- [x] 1.4 在 electron/preload.ts 中暴露编辑器相关 IPC 方法（接收截图数据、发送编辑结果、取消编辑）
- [x] 1.5 修改选择器窗口确认流程：选区后关闭选择器，创建编辑器窗口并传递截图数据
- [x] **Review**: 检查 IPC 通信是否正确、窗口创建参数是否合理、删除冗余代码和兜底逻辑

## 2. 编辑器组件架构

- [x] 2.1 创建 ScreenEditor 主组件，包含工具栏和画布区域
- [x] 2.2 创建 EditorToolbar 组件，包含工具按钮（画笔、文字、箭头、矩形、圆形、马赛克、撤销、重做、取消、确认）
- [x] 2.3 创建 EditorCanvas 组件，初始化 fabric.js Canvas 并加载截图图片
- [x] 2.4 实现编辑器窗口大小自适应截图比例（最大不超过屏幕 80%）
- [x] 2.5 创建编辑器样式文件 src/styles/editor.css
- [x] **Review**: 检查组件职责是否清晰、样式是否与项目风格一致、删除冗余状态和临时代码

## 3. 画笔工具

- [x] 3.1 实现画笔模式切换：选中画笔工具时启用 fabric.js freeDrawingBrush
- [x] 3.2 实现画笔大小配置（提供 3-5 个预设大小）
- [x] 3.3 实现画笔颜色配置（默认黑色，可从调色板选择）
- [x] **Review**: 检查画笔交互是否流畅、删除冗余配置项、审查性能（大量笔画时是否有卡顿）

## 4. 文字工具

- [x] 4.1 实现文字模式：点击画布创建 fabric.IText 可编辑文本
- [x] 4.2 实现文字样式配置（字号、颜色）
- [x] 4.3 实现文字确认逻辑：点击其他区域或按 Enter 确认文字，文本变为不可编辑
- [x] **Review**: 检查文字编辑交互是否自然、删除冗余逻辑、审查中文输入兼容性

## 5. 形状工具

- [x] 5.1 实现箭头工具：拖拽绘制箭头（fabric.Line + 三角形箭头）
- [x] 5.2 实现矩形工具：拖拽绘制矩形轮廓（fabric.Rect）
- [x] 5.3 实现圆形工具：拖拽绘制椭圆（fabric.Ellipse）
- [x] 5.4 实现形状颜色和线宽配置
- [x] **Review**: 检查形状绘制交互是否正确（拖拽方向、最小尺寸限制）、删除重复绘制逻辑、审查渲染性能

## 6. 马赛克工具

- [x] 6.1 实现马赛克模式：将画布转换为像素操作模式
- [x] 6.2 实现马赛克笔刷：拖拽区域应用像素化效果（对选区像素进行降采样再放大）
- [x] 6.3 实现马赛克笔刷大小配置
- [x] **Review**: 检查马赛克效果是否正确、删除冗余像素处理逻辑、审查大数据量性能（使用 requestAnimationFrame 优化）

## 7. 撤销/重做

- [x] 7.1 实现操作历史栈（最大 50 步）
- [x] 7.2 实现撤销功能（Ctrl+Z 或按钮点击）
- [x] 7.3 实现重做功能（Ctrl+Y 或按钮点击）
- [x] 7.4 实现历史栈溢出处理：超出限制时丢弃最早操作
- [x] **Review**: 检查撤销/重做是否覆盖所有工具操作、删除冗余历史记录逻辑、审查内存占用

## 8. 导出与集成

- [x] 8.1 实现确认按钮：将 canvas 导出为 dataURL，通过 IPC 发送回主进程
- [x] 8.2 实现导出图片压缩：超过 1MB 时自动压缩（复用现有 exportCanvasWithinLimit 逻辑）
- [x] 8.3 实现取消按钮：关闭编辑器窗口，不发送数据
- [x] 8.4 实现 Escape 键取消功能
- [x] 8.5 修改主进程：编辑器确认后将图片数据传递给渲染进程预览
- [x] 8.6 确保编辑器窗口关闭时正确清理资源（fabric.js Canvas、IPC 监听器）
- [x] **Review**: 端到端测试完整流程（截图→选区→编辑→确认→预览→发送）、删除冗余清理逻辑、审查 IPC 数据传递是否可靠

## 9. 最终审查

- [x] 9.1 全局代码审查：删除所有未使用的 import、变量、函数
- [x] 9.2 检查所有中文字符串是否正确显示，无乱码
- [x] 9.3 检查编辑器在不同截图尺寸下的表现（小图、大图、超宽图）
- [x] 9.4 检查 TypeScript 类型是否完整，无 any 类型滥用
- [x] 9.5 验证打包后编辑器窗口功能正常（desktop:build 测试）
